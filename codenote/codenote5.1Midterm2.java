public class ArrayReview{
	for (int = 0; i< foobar.length;i++{
	System.out.printf("value is %s\n",Arrays.deepToString(foobar[i])); 
	}
	for (String[]subArray : foobar){
		System.out.printf("value is %s\n",Arrays.deepToString(foobar[i]));
	}

	//ragged array: array with subarray of different length
	String [][] foobar = {
		{"foo","hello","goodbye"},
		{"foo","hello"},
		{"foo"},
		{"foo","hello","goodbye"},
	};
}

